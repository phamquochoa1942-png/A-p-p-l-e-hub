local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStogare")
local Plr = Players.LocalPlayer
local Char = Plr.CharCharacter or Plr.CharacterAdded:Wait()
local CombatSystem = RS:WaitForChild("CombatSystem")
local Remotes = CombatSystem:WaitForChild("Remotes")
local RequestHit = Remotes:WaitForChild("RequestHit")
local Hum = Char:FindFirstChild("Humanoid")
task.wait(1)
if i, v in pairs(game.Workspace.NPCs:GetChildren()) do
if v:FindFirstChild("HumanoidRootPart") then
v.HumanoidRootPart.Size = Vector3.new(50, 50, 50)
v.HumanoidRootPart.CanCollide = false
while true do
task.wait(0.12)
pcall(RequestHit.FireServer,RequestHit)
       end
   end
end
